## 相册

generate_json.bat 用于在子相册中生成 media.json。

sigal.conf.py 配置文件。

https://github.com/zgshen/sigal 安装到虚拟环境
```
python3 -m venv venv
source venv/bin/activate
pip install -e .
```

编译程序由 https://github.com/zgshen/sigal， 本项目根目录执行：
```
xx/sigal/venv/bin/sigal build
```
